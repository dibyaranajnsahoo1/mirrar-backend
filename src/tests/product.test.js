const chai = require('chai');
const chaiHttp = require('chai-http');
const app = require('../App');

chai.use(chaiHttp);
const expect = chai.expect;

describe('Product API', () => {
  // Write tests for model, endpoints, and search functionality
});
